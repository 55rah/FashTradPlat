SELECT Product_name, Category_Name
FROM Products
JOIN Categories ON category_id = category_id;
/* This sql statement joins the category name from the categories table and the product name from the product table into one result*/